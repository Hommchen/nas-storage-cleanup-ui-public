# Security Policy

NAS 清理台会接触媒体文件、qBittorrent 任务、MoviePilot 数据和清理执行链，因此安全问题按高优先级处理。

## 报告安全问题

请不要在公开 Issue 中提交以下内容：

- 可导致未授权删除、越权访问或绕过确认门禁的问题；
- Cookie、passkey、tracker、infohash、控制令牌等敏感数据；
- 可识别真实 NAS 路径、私有站账号或媒体库存的日志。

如果问题不涉及敏感信息，可以创建普通 Issue，并提供最小可复现示例和脱敏日志。涉及敏感安全材料时，请通过 GitHub 仓库提供的私密安全报告渠道联系维护者（如果仓库已启用 Private vulnerability reporting）。

## 安全设计原则

项目默认遵循以下原则：

- fail closed：状态不明确时拒绝执行；
- 默认只读，执行能力需要显式启用；
- 删除前重新刷新并验证资源指纹；
- 文件删除受允许根目录、硬链接、符号链接和占用状态约束；
- 私有运行数据不进入公开快照、浏览器响应或仓库；
- 未完成事务会阻止新的清理操作，直到恢复状态明确。

修复安全问题时，请优先增加回归测试，证明失败路径仍然保持关闭。

## 支持范围

当前主要维护默认分支上的最新版本。历史版本不承诺单独提供安全补丁；建议先在只读模式验证最新版本后再升级生产部署。
